pipeline {
    agent any

    stages {
        stage('git repo') {
            steps {
                git branch: 'main', url: 'https://github.com/YourAWS-Bilal/TipTop'
            }
        }
    }
}
